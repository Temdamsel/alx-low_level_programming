#include <stdio.h>

/**
 * main - Prints the alphabet in lowercase, except for q and e
 *
 * Return: 0
 */
int main(void)
{
	int ch;

	for (ch = 'a'; ch <= 'z'; ch++)
	{
		if (ch != 'e' && ch != 'q')
		{
			putchar(letter);
	}
	else
	{
	}
	}
	putcher('\n');

	return (0);
}
